需求：
    记录会议室创建过程，记录参会人员的入会出会记录。
    提供会议室创建角色的进入会议室接口，提供参会人员的入会接口。
    列出创建会议者的创建的所有的会议室，并提供关闭会议室功能。
    列出以创建的所有会议室，并可根据创建者进行检索，同时提供参会接口。
功能实现：
    1、记录创建过程
        在创建页面将数据存入数据库。
        在现有信息基础上增加加入会议密码，目的在于任何人在加入此会议室时需输入此密码。
    2、入会出会记录
        在加入会议室的地方记录参会信息，在浏览器关闭或点击系统提供的退出按钮后记录出会信息。
    3、列出创建者的会议室列表
        单独实现一个列表，只显示当前登录用户已创建的会议室列表，同时将会议室的创建功能连接加入此页面。
        列表支持分页、按开会时间、会议主题、会议是否已完成等查询条件。
    4、列出所有所有会议室
        列出所有会议室，可通过创建会议者名称、开会时间、会议主题等进行检索，并支持分页。

